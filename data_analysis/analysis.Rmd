---
title: "projet_ER01"
author: "vlutfall"
date: "December 1, 2016"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

loading the library ggplot2 for nice plot functions
```{r}
library(ggplot2)
```

loading our data from a csv file
```{r}
sort_data = read.csv("../results.csv")
summary(sort_data)
str(sort_data)

```

first ploting function, it is not very clear
```{r}

ggplot( data = sort_data, aes(x=Input_size, y=Execution_time, color=factor(Options))) + geom_point() + scale_x_log10() + scale_y_log10() + facet_wrap(~ Language)
```

filtering by language, or size, or anything
```{r}

data_cpp = subset(sort_data, Language == "CPP")


str(data_cpp)

ggplot( data = data_cpp, aes(x= Input_size, y = Execution_time, color=Options)) + geom_point() + scale_x_log10() + scale_y_log10() + theme_bw() + geom_smooth() +


```

```{r}

```

